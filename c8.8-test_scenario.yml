name: C88 Experiment
author: Moritz Schwarz <moritz.schwarz@envite.de>
description: usage_scenario for C8.8 Experiment

compose-file: !include docker-compose-c8.8-single-broker.yaml

services:
  experiment:
    command: /bin/sh

flow:
  - name: runC8Experiment
    container: experiment
    commands:
      - type: console
        command: java -jar demo.jar
        log-stdout: true
        log-stderr: true